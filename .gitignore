__pycache__/
*.pyc
.env
.env.*
!.env.example
venv/
.venv/
node_modules/
uploads/
*.pdf
!tests/*.pdf

# Secrets (belt-and-suspenders — never commit real credentials)
*.pem
*.key
secrets.*
credentials.json

# Generated eval artifacts + fetched datasets (regenerated; numbers live in docs/PROGRESS.md)
backend/eval/results/
backend/eval/data/*/
!backend/eval/data/sample/

# Local model / HF caches (should live in the user cache, never the repo)
.cache/
*.onnx

# Tooling + OS noise
.pytest_cache/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/

# PAN-2023 style-change corpus: wrong task for PRISM (ADR-0016) and not ours to redistribute.
# Kept on disk for the legacy research record only — never tracked.
pan23-multi-author-analysis/
research/datasets/pan/

# Editor / assistant tooling that is not part of the product
.gemini/
.claude/

# E2E screenshots (regenerated every run)
e2e/shots/
