[pytest]
# Only collect the real test package — never the legacy manual scripts
# (test_pipeline.py / _smoketest*.py, which spin up servers / do network).
testpaths = tests
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
