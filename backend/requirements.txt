# P.R.I.S.M. — runtime dependencies (the Originality Checker only; legacy engine removed, ADR-0018).
# Exact pins for reproducible installs; `requirements.lock` is the fully-resolved transitive set.

# Web
fastapi==0.115.0
pydantic-settings==2.15.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
python-dotenv==1.0.1

# PDF / text extraction
PyMuPDF==1.28.2
pdfplumber==0.11.10

# Matching (verbatim + paraphrase + translated)
numpy==1.26.4
scikit-learn==1.9.0
# Heavy (pulls torch). Paraphrase matching degrades gracefully to verbatim-only if unavailable.
sentence-transformers==6.0.0
langdetect==1.0.9

# Academic corpus (OpenAlex + arXiv)
requests==2.32.5
arxiv==2.1.3
